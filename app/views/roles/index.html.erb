<div class="index">

  <% provide(:title, 'Roles') %>

  <h1>Roles</h1>
  <p>See a list of all roles in the database:</p>

  <table class="all-db-entries">
    <%= render partial: 'layouts/list_roles_movies', locals: {:roles => @roles}%>
  </table>

  <div class="btn btn-add">
    <%= link_to "add role", new_role_path %>
  </div>

</div>